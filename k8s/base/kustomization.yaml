apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - crd.yaml        
  - operator.yaml
  - site.yaml

# override the image tag
images:
  - name: rust-operator
    newName: rust-operator
    newTag: dev
